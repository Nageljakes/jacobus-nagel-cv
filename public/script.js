const reduced=window.matchMedia('(prefers-reduced-motion: reduce)').matches;const items=document.querySelectorAll('.fade-in-up');if(reduced){items.forEach(x=>x.classList.add('visible'))}else{const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');observer.unobserve(e.target)}}),{threshold:.12});items.forEach(x=>observer.observe(x))}const toggle=document.querySelector('.nav-toggle');const links=document.querySelector('.nav-links');const setNav=open=>{links.classList.toggle('open',open);toggle.setAttribute('aria-expanded',String(open));toggle.setAttribute('aria-label',open?'Close navigation':'Open navigation');toggle.textContent=open?'Close':'Menu'};toggle.addEventListener('click',()=>setNav(!links.classList.contains('open')));links.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>setNav(false)));document.querySelectorAll('.video-frame a[data-video-id] img').forEach(img=>{const id=img.closest('a').dataset.videoId;const probe=new Image();probe.onload=()=>{if(probe.naturalHeight>probe.naturalWidth)img.src=probe.src};probe.src=`https://i.ytimg.com/vi/${id}/oardefault.jpg`});const details=[...document.querySelectorAll('details')];addEventListener('beforeprint',()=>details.forEach(d=>{d.dataset.wasOpen=d.open;d.open=true}));addEventListener('afterprint',()=>details.forEach(d=>{if(d.dataset.wasOpen==='false')d.open=false}));document.querySelectorAll('.video-frame a[data-video-id]').forEach(a=>a.addEventListener('click',e=>{e.preventDefault();const id=a.dataset.videoId;const iframe=document.createElement('iframe');iframe.src=`https://www.youtube-nocookie.com/embed/${id}?autoplay=1&rel=0`;iframe.title=a.getAttribute('aria-label')||'YouTube video player';iframe.allow='accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share';iframe.allowFullscreen=true;iframe.loading='lazy';a.replaceWith(iframe)}));
